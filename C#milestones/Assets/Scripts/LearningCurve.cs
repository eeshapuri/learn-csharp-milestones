using UnityEngine;

public class LearningCurve : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int myInt = 62;
        float myFloat = 7.76543f;
        string myString = "Hey";
        bool myBool = true;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}